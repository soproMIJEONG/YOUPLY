import React from 'react';
import VideoItem from './VideoItem';

const VideoList = ({ buttonHandler }) => {
    return (
        <button onClick={buttonHandler}>BUTTON</button>
    );
};
export default VideoList;